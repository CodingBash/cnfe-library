
core_processing <- function(){
  inputRegions <- NA # Input segments or events
  inputBoundaries <- NA # Input genome boundaries (determined by chromosome sizes usually)
  
  # *** 
  
  coreTable <- NA # CORE results
}

core_selection <- function(){
  coreTable <- NA # Input coreTable
  
  # . . . selection based on conditional
  
  coreSelection <- NA # Output coreSelection
}
